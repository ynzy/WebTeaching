var arr = ['red', 'green', 'blue'];
// length 数组的长度
// console.log(arr.length);

/* for (var i = 0; i < arr.length; i++) { 
  if (arr[i] == 'red') {
    console.log(arr[i]);
  } else if (i == 1) { 
    console.log('i=1:'+arr[i]);
  }
}
 */

arr.length = 4
console.log(arr);
arr.length = 2
console.log(arr);

